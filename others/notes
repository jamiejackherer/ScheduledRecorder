generale:
refactoring dell’app usando gli architecture components, mvvm pattern, data binding e test
refactoring della UI
pubblicazione nel play store


audio view:
- quando l'app va in background (aprendo un'altra app) e ritornando la scala dei secondi è sbagliata
  (vedi metodo startRecording che aggiorni anche la scala)
- migliorare precisione scala secondi quando si ritorno al record fragment
- provare buffer di amplitudes per migliorare effetto grafico


problem fix:
1) cannot resolve symbol theme in styles.xml: chiudere progetto; eliminarlo dalla
   lista di sx; open existing as project